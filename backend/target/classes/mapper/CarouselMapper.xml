<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.campus.bookborrow.mapper.CarouselMapper">

    <select id="selectAll" resultType="com.campus.bookborrow.entity.Carousel">
        SELECT * FROM carousel ORDER BY sort_order ASC
    </select>

    <select id="selectEnabled" resultType="com.campus.bookborrow.entity.Carousel">
        SELECT * FROM carousel WHERE status = 1 ORDER BY sort_order ASC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO carousel (image_url, link_url, title, sort_order, status)
        VALUES (#{imageUrl}, #{linkUrl}, #{title}, #{sortOrder}, #{status})
    </insert>

    <update id="update">
        UPDATE carousel SET image_url=#{imageUrl}, link_url=#{linkUrl},
        title=#{title}, sort_order=#{sortOrder}, status=#{status}
        WHERE id=#{id}
    </update>

    <delete id="deleteById">
        DELETE FROM carousel WHERE id=#{id}
    </delete>
</mapper>
